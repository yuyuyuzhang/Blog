const num = null;
const arr = [1, 2, 3];

if(num) {
    for(let i=0; i<num.length; i++) {
        console.log(num[i]);
    }
}

if(arr) {
    for(let i=0; i<arr.length; i++) {
        console.log(arr[i]);
    }
}